import { DashBoardType } from "@/types";
import { BookMarkedIcon, ListOrdered, FileTextIcon as PostIcon } from "lucide-react";

const dashBoardBookMark: DashBoardType = {
  title: "Orders",
  slug: "/bookmarks",
  icon: ListOrdered,
};

export default dashBoardBookMark;
